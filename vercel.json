:root {
  --bg: #F5F7FB;
  --surface: #FFFFFF;
  --surface-soft: #F1F4FF;
  --text: #172033;
  --muted: #7D879D;
  --line: #E6EAF2;
  --primary: #4F6BFF;
  --primary-dark: #394ED8;
  --green: #188C68;
  --red: #D95050;
  --amber: #A96900;
  --shadow: 0 14px 40px rgba(36, 49, 92, .09);
  --radius-xl: 28px;
  --radius-lg: 20px;
  --radius-md: 14px;
  font-family: Pretendard, "Noto Sans KR", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

* { box-sizing: border-box; }

html, body {
  margin: 0;
  min-height: 100%;
  background: var(--bg);
  color: var(--text);
}

body { overscroll-behavior-y: none; }

button, input, select, textarea { font: inherit; }
button { cursor: pointer; }

#app {
  max-width: 520px;
  min-height: 100vh;
  margin: 0 auto;
  background: var(--bg);
}

.topbar {
  position: sticky;
  top: 0;
  z-index: 20;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: calc(18px + env(safe-area-inset-top)) 20px 14px;
  background: rgba(245, 247, 251, .92);
  backdrop-filter: blur(18px);
}

.topbar h1, .section-heading h2, .section-heading h3, .dialog-heading h2 { margin: 0; }
.topbar h1 { font-size: 22px; }

.eyebrow {
  margin: 0 0 4px;
  color: var(--primary);
  font-size: 11px;
  font-weight: 800;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.icon-button {
  width: 42px;
  height: 42px;
  border: 0;
  border-radius: 14px;
  background: var(--surface);
  box-shadow: 0 8px 24px rgba(36, 49, 92, .08);
}

.content {
  padding: 4px 18px calc(112px + env(safe-area-inset-bottom));
}

.view { display: none; animation: fade .18s ease; }
.view.active { display: block; }

@keyframes fade {
  from { opacity: 0; transform: translateY(3px); }
  to { opacity: 1; transform: translateY(0); }
}

.hero-card {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 24px;
  margin: 8px 0 14px;
  border-radius: var(--radius-xl);
  color: white;
  background:
    radial-gradient(circle at 85% 12%, rgba(255,255,255,.22), transparent 26%),
    linear-gradient(135deg, #526DFF, #715CF4);
  box-shadow: 0 18px 40px rgba(79,107,255,.28);
}

.hero-label { margin: 0 0 8px; font-size: 13px; opacity: .82; }
.hero-card h2 { margin: 0; font-size: 34px; letter-spacing: -.03em; }
.hero-sub { margin: 8px 0 0; font-size: 12px; opacity: .78; }
.hero-badge {
  min-width: 48px;
  padding: 9px 11px;
  text-align: center;
  border-radius: 999px;
  background: rgba(255,255,255,.18);
  font-weight: 800;
  font-size: 12px;
}


.home-user-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  margin: 0 0 12px;
  padding: 14px 15px;
  border: 1px solid #E4E8F2;
  border-radius: 19px;
  background: var(--surface);
  box-shadow: 0 8px 24px rgba(36,49,92,.055);
  transition: border-color .18s ease, background .18s ease, box-shadow .18s ease;
}

.home-user-card.needs-selection {
  border-color: #AEB9FF;
  background: #F8F9FF;
  box-shadow: 0 8px 24px rgba(79,107,255,.09);
}

.home-user-profile {
  display: flex;
  align-items: center;
  min-width: 0;
  gap: 11px;
}

.home-user-avatar {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 14px;
  color: white;
  background: linear-gradient(135deg, #667CFF, #785CF3);
  box-shadow: 0 7px 16px rgba(79,107,255,.22);
  font-size: 16px;
  font-weight: 850;
}

.home-user-card.needs-selection .home-user-avatar {
  color: var(--primary);
  background: var(--surface-soft);
  box-shadow: none;
}

.home-user-copy {
  min-width: 0;
}

.home-user-copy > span,
.home-user-copy > strong,
.home-user-copy > small {
  display: block;
}

.home-user-copy > span {
  margin-bottom: 2px;
  color: var(--primary);
  font-size: 10px;
  font-weight: 850;
}

.home-user-copy > strong {
  overflow: hidden;
  margin-bottom: 2px;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 14px;
}

.home-user-copy > small {
  display: -webkit-box;
  overflow: hidden;
  color: var(--muted);
  font-size: 10px;
  line-height: 1.4;
  white-space: normal;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.home-user-select-wrap {
  flex: 0 0 126px;
}

.home-user-select-wrap select {
  min-height: 42px;
  padding: 0 34px 0 11px;
  border-color: #DCE2F0;
  border-radius: 13px;
  background-color: #F8F9FC;
  font-size: 12px;
  font-weight: 750;
}

.home-user-card.needs-selection .home-user-select-wrap select {
  border-color: #9EACFF;
  color: var(--primary);
  background-color: white;
}

@media (max-width: 390px) {
  .home-user-card {
    align-items: stretch;
    flex-direction: column;
    gap: 11px;
  }

  .home-user-select-wrap {
    flex-basis: auto;
    width: 100%;
  }

  .home-user-copy > small {
    white-space: normal;
    line-height: 1.4;
  }
}

.summary-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

.summary-card {
  display: flex;
  min-height: 94px;
  flex-direction: column;
  justify-content: space-between;
  padding: 16px;
  border-radius: var(--radius-lg);
  background: var(--surface);
  box-shadow: 0 8px 28px rgba(36, 49, 92, .06);
}
.summary-card span { color: var(--muted); font-size: 12px; }
.summary-card strong { font-size: 18px; }
.summary-card.accent-card {
  grid-column: 1 / -1;
  min-height: 80px;
  border: 1px solid transparent;
  background: var(--surface-soft);
  transition: color .18s ease, border-color .18s ease, background .18s ease;
}

.summary-card.accent-card.balance-state-in {
  border-color: #D7EEE6;
  background: #F0FAF6;
}

.summary-card.accent-card.balance-state-out {
  border-color: #DDD5FA;
  background: #F5F2FF;
}

.summary-card.accent-card.balance-state-even {
  border-color: #E3E7F8;
  background: #F4F6FF;
}

.section-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin: 26px 2px 12px;
}
.section-heading.mini { margin-top: 22px; }
.section-heading.mini h3 { font-size: 16px; }
.sticky-heading { position: relative; }

.text-button, .danger-link {
  padding: 6px 2px;
  border: 0;
  background: transparent;
  color: var(--primary);
  font-size: 13px;
  font-weight: 700;
}
.text-button.muted { color: var(--muted); }
.danger-link { color: var(--red); }

.primary-button, .secondary-button, .small-button, .danger-button {
  border: 0;
  border-radius: 14px;
  font-weight: 800;
}
.primary-button {
  padding: 14px 18px;
  color: white;
  background: var(--primary);
}
.primary-button:hover { background: var(--primary-dark); }
.primary-button.full { width: 100%; margin-top: 8px; }
.secondary-button { padding: 13px 16px; color: var(--primary); background: var(--surface-soft); }
.small-button { padding: 10px 13px; color: white; background: var(--primary); }
.danger-button { width: 100%; padding: 12px; color: var(--red); background: #FFF0F0; }

.expense-list { display: grid; gap: 10px; }
.expense-item {
  display: grid;
  grid-template-columns: 44px minmax(0, 1fr) auto 14px;
  align-items: center;
  gap: 11px;
  padding: 14px;
  border: 1px solid transparent;
  border-radius: 18px;
  background: var(--surface);
  box-shadow: 0 8px 24px rgba(36, 49, 92, .055);
}
.expense-item:active { border-color: #CCD4FF; }
.category-icon {
  display: grid;
  place-items: center;
  width: 44px;
  height: 44px;
  border-radius: 14px;
  background: var(--surface-soft);
  font-size: 21px;
}
.expense-main { min-width: 0; }
.expense-main strong, .expense-main span { display: block; }
.expense-main strong {
  overflow: hidden;
  margin-bottom: 5px;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 14px;
}
.expense-main span { color: var(--muted); font-size: 11px; }
.expense-amount { text-align: right; }
.expense-amount strong { display: block; font-size: 14px; }
.expense-amount span { color: var(--muted); font-size: 11px; }
.expense-chevron {
  color: #B1B8C8;
  font-size: 23px;
  font-weight: 400;
  line-height: 1;
}

.empty-state {
  padding: 30px 20px;
  text-align: center;
  border: 1px dashed #D7DDEA;
  border-radius: 22px;
  background: rgba(255,255,255,.55);
}
.empty-state.compact { padding: 24px 16px; }
.empty-state p { max-width: 300px; margin: 8px auto 18px; color: var(--muted); font-size: 13px; line-height: 1.6; }
.empty-icon { margin-bottom: 8px; font-size: 28px; }

.filter-panel {
  display: grid;
  gap: 8px;
  padding: 12px;
  margin-bottom: 12px;
  border-radius: 18px;
  background: var(--surface);
}
.filter-row { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }

input, select, textarea {
  width: 100%;
  border: 1px solid var(--line);
  border-radius: 13px;
  outline: none;
  background: #FAFBFD;
  color: var(--text);
}
input, select { min-height: 46px; padding: 0 13px; }
textarea { padding: 12px 13px; resize: vertical; }
input:focus, select:focus, textarea:focus { border-color: #9DABFF; box-shadow: 0 0 0 3px rgba(79,107,255,.10); }

.form-card {
  display: block;
  padding: 20px;
  border: 1px solid rgba(230, 234, 242, .72);
  border-radius: 26px;
  background: var(--surface);
  box-shadow: 0 14px 36px rgba(36, 49, 92, .075);
}

.form-field {
  display: block;
  min-width: 0;
  margin: 0 0 22px;
}

.form-grid.form-field {
  margin-bottom: 22px;
}

.form-grid .compact-field {
  margin-bottom: 0;
}

.field-label,
.dialog-shell label > span,
legend {
  display: block;
  margin: 0 0 9px;
  color: var(--text);
  font-size: 13px;
  font-weight: 850;
  line-height: 1.35;
}

.field-label-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  margin-bottom: 9px;
}

.field-label-row .field-label {
  margin: 0;
}

.field-label-row > small {
  padding: 4px 8px;
  border-radius: 999px;
  color: var(--muted);
  background: #F2F4F8;
  font-size: 10px;
  font-weight: 750;
  white-space: nowrap;
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

.money-input { position: relative; }
.money-input input {
  padding-right: 42px;
  font-size: 21px;
  font-weight: 850;
}
.money-input b {
  position: absolute;
  top: 50%;
  right: 14px;
  transform: translateY(-50%);
}

fieldset {
  min-width: 0;
  padding: 0;
  margin: 0;
  border: 0;
}

.form-section {
  padding-top: 2px;
}

.field-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 10px;
}

.field-heading legend {
  width: auto;
  margin: 0;
}

.participant-actions {
  display: flex;
  flex-shrink: 0;
  gap: 6px;
}

.mini-action {
  min-height: 30px;
  padding: 0 9px;
  border: 0;
  border-radius: 999px;
  color: var(--muted);
  background: #F1F3F8;
  font-size: 11px;
  font-weight: 800;
}

.mini-action.active {
  color: var(--primary);
  background: var(--surface-soft);
}

.chip-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.participant-chip input { display: none; }
.participant-chip span {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 42px;
  margin: 0;
  padding: 0 15px;
  border: 1px solid #DCE1EE;
  border-radius: 999px;
  background: #F8F9FC;
  color: #677188;
  font-size: 13px;
  font-weight: 750;
  transition: border-color .16s ease, background .16s ease, color .16s ease, transform .16s ease;
}

.participant-chip input:checked + span {
  border-color: #91A2FF;
  background: #EEF1FF;
  color: var(--primary);
  box-shadow: inset 0 0 0 1px rgba(79,107,255,.04);
}

.participant-chip input:checked + span:active {
  transform: scale(.97);
}

.segment-control {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4px;
  padding: 4px;
  border-radius: 16px;
  background: #EEF1F7;
}

.segment-control label {
  display: block;
  min-width: 0;
}

.segment-control input { display: none; }
.segment-control span {
  display: grid;
  place-items: center;
  min-height: 46px;
  margin: 0;
  padding: 0 12px;
  border-radius: 12px;
  color: #7A8398;
  font-size: 13px;
  font-weight: 820;
  white-space: nowrap;
  transition: background .16s ease, color .16s ease, box-shadow .16s ease;
}

.segment-control input:checked + span {
  background: white;
  color: var(--primary);
  box-shadow: 0 5px 14px rgba(36,49,92,.09);
}

.custom-split {
  display: grid;
  gap: 8px;
  margin-top: 11px;
}

.custom-row {
  display: grid;
  grid-template-columns: 1fr 120px;
  align-items: center;
  gap: 10px;
  padding: 8px 0;
}

.custom-row strong { font-size: 13px; }
.custom-row input {
  min-height: 42px;
  text-align: right;
}

.hint {
  margin: 10px 2px 0;
  color: var(--muted);
  font-size: 11px;
  line-height: 1.55;
}

.form-error {
  margin: 0 0 14px;
  padding: 10px 12px;
  border-radius: 12px;
  color: var(--red);
  background: #FFF2F2;
  font-size: 12px;
  font-weight: 750;
  line-height: 1.45;
}

textarea {
  min-height: 112px;
  line-height: 1.55;
}

.receipt-field {
  margin-bottom: 14px;
}

.receipt-picker-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 9px;
}

.receipt-option {
  display: flex;
  align-items: center;
  min-width: 0;
  min-height: 72px;
  gap: 10px;
  padding: 12px;
  border: 1px solid #E1E5EF;
  border-radius: 16px;
  color: var(--text);
  background: #FAFBFD;
  text-align: left;
  transition: transform .16s ease, border-color .16s ease, background .16s ease;
}

.receipt-option:active {
  transform: scale(.98);
  border-color: #A5B2FF;
  background: #F3F5FF;
}

.receipt-option-icon {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 36px;
  height: 36px;
  border-radius: 12px;
  background: var(--surface-soft);
  font-size: 18px;
}

.receipt-option-copy {
  display: block;
  min-width: 0;
}

.receipt-option-copy strong,
.receipt-option-copy small {
  display: block;
}

.receipt-option-copy strong {
  overflow: hidden;
  margin-bottom: 3px;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 12px;
  font-weight: 820;
}

.receipt-option-copy small {
  overflow: hidden;
  color: var(--muted);
  font-size: 10px;
  line-height: 1.35;
}

.receipt-selection-text {
  margin: 9px 2px 0;
  color: var(--muted);
  font-size: 11px;
}

.receipt-selection-text.has-file {
  color: var(--primary);
  font-weight: 750;
}

.visually-hidden {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important;
  white-space: nowrap !important;
  border: 0 !important;
}

.receipt-preview {
  overflow: hidden;
  margin: 0 0 18px;
  padding: 10px;
  border: 1px solid #E7EAF2;
  border-radius: 18px;
  background: #F7F8FC;
}

.receipt-preview-scroll {
  max-height: min(54vh, 520px);
  overflow-x: hidden;
  overflow-y: auto;
  overscroll-behavior: contain;
  border-radius: 13px;
  background:
    linear-gradient(45deg, #F2F3F6 25%, transparent 25%),
    linear-gradient(-45deg, #F2F3F6 25%, transparent 25%),
    linear-gradient(45deg, transparent 75%, #F2F3F6 75%),
    linear-gradient(-45deg, transparent 75%, #F2F3F6 75%);
  background-position: 0 0, 0 6px, 6px -6px, -6px 0;
  background-size: 12px 12px;
  -webkit-overflow-scrolling: touch;
}

.receipt-preview img {
  display: block;
  width: 100%;
  height: auto;
  max-height: none;
  object-fit: initial;
  border-radius: 13px;
  background: white;
  image-rendering: auto;
  touch-action: pan-y pinch-zoom;
}

.receipt-preview-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 9px 2px 0;
  color: var(--muted);
  font-size: 10px;
  line-height: 1.4;
}

.receipt-preview-footer > span {
  min-width: 0;
}

.save-expense-button {
  min-height: 56px;
  margin-top: 2px !important;
  border-radius: 16px;
  font-size: 16px;
}

.settlement-total-card {
  padding: 22px;
  border-radius: 24px;
  color: white;
  background: linear-gradient(135deg, #172033, #354260);
}
.settlement-total-card span { font-size: 12px; opacity: .72; }
.settlement-total-card strong { display: block; margin: 8px 0; font-size: 30px; }
.settlement-total-card p { margin: 0; font-size: 12px; opacity: .7; }

.person-summary, .transfer-list { display: grid; gap: 9px; }
.person-row, .transfer-row {
  display: grid;
  align-items: center;
  gap: 10px;
  padding: 14px;
  border-radius: 17px;
  background: var(--surface);
}
.person-row { grid-template-columns: 1fr auto; }
.person-numbers { display: flex; gap: 12px; color: var(--muted); font-size: 11px; }
.person-balance { text-align: right; }
.person-balance strong { display: block; font-size: 14px; }
.person-balance span { font-size: 11px; color: var(--muted); }
.balance-positive { color: var(--green); }
.balance-negative { color: #6854C7; }

.transfer-row.my-transfer {
  border: 1px solid #DED8FA;
  background: #F7F5FF;
}

.transfer-row.my-transfer .transfer-arrow,
.transfer-row.my-transfer > strong {
  color: #6854C7;
}

.transfer-row { grid-template-columns: 1fr auto; }
.transfer-route { display: flex; align-items: center; gap: 8px; font-size: 13px; font-weight: 800; }
.transfer-arrow { color: var(--primary); }
.transfer-row strong { color: var(--primary); }
.badge {
  padding: 5px 8px;
  border-radius: 999px;
  background: #E9EDFF;
  color: var(--primary);
  font-size: 10px;
  font-weight: 800;
}

.bottom-nav {
  position: fixed;
  left: 50%;
  bottom: 0;
  z-index: 30;
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  align-items: center;
  width: min(520px, 100%);
  min-height: 78px;
  padding: 8px 10px calc(8px + env(safe-area-inset-bottom));
  transform: translateX(-50%);
  border-top: 1px solid rgba(230,234,242,.9);
  background: rgba(255,255,255,.96);
  backdrop-filter: blur(18px);
}

.nav-item,
.nav-add {
  border: 0;
  background: transparent;
}

.nav-item {
  display: grid;
  min-width: 0;
  min-height: 54px;
  place-items: center;
  align-content: center;
  gap: 3px;
  color: #A0A8B8;
  font-size: 18px;
}

.nav-item span {
  line-height: 1;
}

.nav-item b {
  font-size: 10px;
  font-weight: 750;
}

.nav-item.active { color: var(--primary); }

.nav-add {
  justify-self: center;
  align-self: center;
  width: 58px;
  height: 58px;
  margin-top: -30px;
  border-radius: 20px;
  color: white;
  background: var(--primary);
  box-shadow: 0 13px 25px rgba(79,107,255,.35);
  font-size: 30px;
  line-height: 1;
}

.top-settings-button {
  display: none;
}

.sheet-dialog {
  width: min(520px, 100%);
  max-width: none;
  padding: 0;
  margin: auto auto 0;
  border: 0;
  border-radius: 26px 26px 0 0;
  background: transparent;
}
.sheet-dialog[open]::backdrop {
  background: rgba(18,25,42,.42);
  backdrop-filter: blur(4px);
  animation: sheet-backdrop-in .24s ease-out both;
}
.sheet-dialog[open] .dialog-shell {
  animation: sheet-slide-in .36s cubic-bezier(.22, 1, .36, 1) both;
}
.dialog-shell {
  max-height: 88vh;
  overflow-y: auto;
  overscroll-behavior: contain;
  padding: 12px 20px calc(24px + env(safe-area-inset-bottom));
  border-radius: 26px 26px 0 0;
  background: var(--surface);
  box-shadow: 0 -18px 54px rgba(18,25,42,.14);
  will-change: transform, opacity;
}

@keyframes sheet-slide-in {
  from {
    opacity: .35;
    transform: translateY(100%);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes sheet-backdrop-in {
  from {
    background: rgba(18,25,42,0);
    backdrop-filter: blur(0);
  }
  to {
    background: rgba(18,25,42,.42);
    backdrop-filter: blur(4px);
  }
}
.dialog-handle {
  width: 42px;
  height: 5px;
  margin: 0 auto 16px;
  border-radius: 999px;
  background: #DCE1EB;
}
.dialog-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20px;
}
.dialog-shell > label, .dialog-shell > fieldset { display: block; margin-bottom: 16px; }
.participant-add-row { display: grid; grid-template-columns: 1fr auto; gap: 8px; }
.manage-list { display: grid; gap: 7px; margin-top: 10px; }
.manage-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 12px;
  border-radius: 13px;
  background: #F7F8FC;
}
.manage-row button { border: 0; background: transparent; color: var(--red); font-weight: 800; }
.dialog-actions { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin: 20px 0 10px; }

.detail-title { margin: 0 0 4px; font-size: 21px; }
.detail-amount {
  margin: 0 0 18px;
  color: #3855E8;
  font-size: 31px;
  font-weight: 900;
  letter-spacing: -.025em;
  line-height: 1.15;
}
.detail-grid { display: grid; gap: 0; }
.detail-row {
  display: grid;
  grid-template-columns: 82px minmax(0, 1fr);
  align-items: start;
  gap: 14px;
  padding: 12px 0;
  border-bottom: 1px solid var(--line);
  font-size: 13px;
}
.detail-row > span {
  color: var(--muted);
  white-space: nowrap;
}
.detail-row > strong {
  min-width: 0;
  text-align: right;
  line-height: 1.55;
  overflow-wrap: anywhere;
}

.detail-split-section {
  padding: 14px 0 13px;
  border-bottom: 1px solid var(--line);
}
.detail-split-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 10px;
}
.detail-split-heading > span {
  color: var(--muted);
  font-size: 13px;
}
.detail-split-heading > small {
  display: inline-flex;
  align-items: center;
  min-height: 24px;
  padding: 0 9px;
  border-radius: 999px;
  color: var(--primary);
  background: var(--surface-soft);
  font-size: 11px;
  font-weight: 800;
}
.detail-split-list {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 8px;
}
.detail-split-item {
  min-width: 0;
  padding: 11px 12px;
  border: 1px solid #E9ECF4;
  border-radius: 14px;
  background: #FAFBFD;
}
.detail-split-item strong,
.detail-split-item span {
  display: block;
  min-width: 0;
}
.detail-split-item strong {
  overflow: hidden;
  margin-bottom: 5px;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 13px;
}
.detail-split-item span {
  color: var(--primary);
  font-size: 13px;
  font-weight: 800;
}

@media (max-width: 359px) {
  .detail-split-list {
    grid-template-columns: 1fr;
  }
}

@media (prefers-reduced-motion: reduce) {
  .sheet-dialog[open]::backdrop,
  .sheet-dialog[open] .dialog-shell {
    animation: none;
  }
}
.detail-receipt-scroll {
  max-height: min(62vh, 640px);
  overflow-x: hidden;
  overflow-y: auto;
  margin-top: 14px;
  border: 1px solid #E7EAF2;
  border-radius: 16px;
  background: #F5F6FA;
  overscroll-behavior: contain;
  -webkit-overflow-scrolling: touch;
}

.detail-receipt {
  display: block;
  width: 100%;
  height: auto;
  max-height: none;
  object-fit: initial;
  border-radius: 15px;
  background: white;
  image-rendering: auto;
  touch-action: pan-y pinch-zoom;
}
.detail-actions {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  gap: 10px;
  margin-top: 18px;
}

.detail-edit-button {
  min-height: 48px;
  border: 0;
  border-radius: 14px;
  color: white;
  background: var(--primary);
  font-weight: 850;
}

.detail-delete-button {
  min-height: 44px;
  padding: 0 13px;
  border: 0;
  color: #C24C5A;
  background: transparent;
  font-weight: 800;
}

.toast {
  position: fixed;
  left: 50%;
  bottom: calc(94px + env(safe-area-inset-bottom));
  z-index: 100;
  max-width: calc(100% - 40px);
  padding: 11px 15px;
  transform: translate(-50%, 20px);
  border-radius: 999px;
  color: white;
  background: rgba(23,32,51,.94);
  opacity: 0;
  pointer-events: none;
  transition: .2s ease;
  font-size: 12px;
  font-weight: 700;
}
.toast.show { opacity: 1; transform: translate(-50%, 0); }
.hidden { display: none !important; }

@media (min-width: 700px) {
  body { padding: 30px 0; }
  #app {
    min-height: calc(100vh - 60px);
    border-radius: 32px;
    overflow: hidden;
    box-shadow: 0 25px 80px rgba(36,49,92,.15);
  }
  .bottom-nav { bottom: 30px; border-radius: 0 0 32px 32px; }
  .sheet-dialog { margin-bottom: 30px; border-radius: 26px; }
  .dialog-shell { border-radius: 26px; }
}

/* Shared Appwrite version */
.topbar-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.sync-status {
  display: inline-flex;
  align-items: center;
  min-height: 28px;
  padding: 0 9px;
  border-radius: 999px;
  color: var(--muted);
  background: rgba(255,255,255,.85);
  box-shadow: 0 6px 18px rgba(36,49,92,.06);
  font-size: 10px;
  font-weight: 800;
  white-space: nowrap;
}

.sync-status.syncing { color: var(--primary); }
.sync-status.success { color: var(--green); }
.sync-status.error { color: var(--red); }
.settings-hint { margin: -8px 0 16px; }

.loading-overlay {
  position: fixed;
  inset: 0;
  z-index: 200;
  display: grid;
  place-items: center;
  padding: 20px;
  background: rgba(245,247,251,.78);
  backdrop-filter: blur(6px);
}

.loading-card {
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 16px 19px;
  border-radius: 18px;
  background: white;
  box-shadow: var(--shadow);
  font-size: 13px;
}

.loading-spinner {
  width: 19px;
  height: 19px;
  border: 3px solid #E3E7FF;
  border-top-color: var(--primary);
  border-radius: 50%;
  animation: spin .8s linear infinite;
}

@keyframes spin { to { transform: rotate(360deg); } }


@media (max-width: 390px) {
  .receipt-picker-actions {
    grid-template-columns: 1fr;
  }

  .receipt-option {
    min-height: 62px;
  }

  .field-heading {
    align-items: flex-start;
    flex-direction: column;
    gap: 7px;
  }
}


/* v2.9 interaction polish */
.balance-summary-card {
  width: 100%;
  text-align: left;
  cursor: pointer;
}

.balance-summary-card:disabled {
  cursor: default;
  opacity: 1;
}

.balance-summary-card small {
  display: block;
  margin-top: 5px;
  color: var(--muted);
  font-size: 10px;
  font-weight: 650;
  line-height: 1.4;
}

.balance-summary-card:not(:disabled):active {
  transform: scale(.99);
}

.home-user-card.is-selected {
  min-height: 60px;
  padding: 10px 12px;
}

.home-user-card.is-selected .home-user-avatar {
  width: 36px;
  height: 36px;
  border-radius: 12px;
  font-size: 14px;
}

.home-user-card.is-selected .home-user-copy > span,
.home-user-card.is-selected .home-user-copy > small {
  display: none;
}

.home-user-card.is-selected .home-user-copy > strong {
  margin: 0;
  font-size: 13px;
}

.home-user-card.is-selected .home-user-select-wrap {
  flex-basis: 112px;
}

.home-user-card.is-selected .home-user-select-wrap select {
  min-height: 38px;
}

.save-expense-button[disabled] {
  cursor: wait;
  opacity: .72;
  box-shadow: none;
}

.sync-status.offline {
  color: #6F7788;
  background: #EEF0F4;
}

.settlement-first-heading {
  margin-top: 22px;
}

.detail-receipt-scroll {
  position: relative;
}

.receipt-fullscreen-button {
  position: sticky;
  right: 10px;
  bottom: 10px;
  float: right;
  z-index: 2;
  min-height: 34px;
  margin: -46px 10px 10px 0;
  padding: 0 11px;
  border: 0;
  border-radius: 999px;
  color: white;
  background: rgba(23,32,51,.82);
  backdrop-filter: blur(8px);
  font-size: 11px;
  font-weight: 800;
}

.receipt-viewer-dialog {
  width: 100vw;
  max-width: none;
  height: 100dvh;
  max-height: none;
  margin: 0;
  padding: 0;
  border: 0;
  background: #10131B;
}

.receipt-viewer-dialog::backdrop {
  background: #10131B;
}

.receipt-viewer-shell {
  display: grid;
  grid-template-rows: auto minmax(0, 1fr) auto;
  height: 100dvh;
  color: white;
  background: #10131B;
}

.receipt-viewer-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: calc(12px + env(safe-area-inset-top)) 16px 12px;
}

.receipt-viewer-toolbar strong,
.receipt-viewer-toolbar small {
  display: block;
}

.receipt-viewer-toolbar strong {
  margin-bottom: 2px;
  font-size: 15px;
}

.receipt-viewer-toolbar small {
  color: #AEB5C5;
  font-size: 10px;
}

.receipt-viewer-close {
  width: 42px;
  height: 42px;
  border: 0;
  border-radius: 14px;
  color: white;
  background: rgba(255,255,255,.1);
  font-size: 20px;
}

.receipt-viewer-stage {
  overflow: auto;
  overscroll-behavior: contain;
  padding: 12px;
  background:
    radial-gradient(circle at 50% 0%, rgba(79,107,255,.12), transparent 38%),
    #10131B;
  -webkit-overflow-scrolling: touch;
  touch-action: pan-x pan-y;
}

.receipt-viewer-stage img {
  display: block;
  width: 100%;
  height: auto;
  min-width: 100%;
  margin: 0 auto;
  border-radius: 10px;
  background: white;
  transform-origin: top center;
  user-select: none;
  -webkit-user-drag: none;
}

.receipt-viewer-controls {
  display: grid;
  grid-template-columns: 54px 80px 54px;
  justify-content: center;
  gap: 8px;
  padding: 12px 16px calc(12px + env(safe-area-inset-bottom));
  background: rgba(16,19,27,.96);
}

.receipt-viewer-controls button {
  min-height: 44px;
  border: 1px solid rgba(255,255,255,.12);
  border-radius: 13px;
  color: white;
  background: rgba(255,255,255,.08);
  font-weight: 800;
}

.confirm-dialog {
  width: min(360px, calc(100% - 36px));
  padding: 0;
  border: 0;
  border-radius: 22px;
  background: transparent;
}

.confirm-dialog::backdrop {
  background: rgba(18,25,42,.48);
  backdrop-filter: blur(5px);
}

.confirm-dialog-card {
  padding: 24px 20px 18px;
  text-align: center;
  border-radius: 22px;
  background: white;
  box-shadow: 0 24px 70px rgba(18,25,42,.2);
}

.confirm-dialog-icon {
  display: grid;
  place-items: center;
  width: 46px;
  height: 46px;
  margin: 0 auto 12px;
  border-radius: 15px;
  color: #6854C7;
  background: #F2EFFF;
  font-size: 21px;
}

.confirm-dialog-card h2 {
  margin: 0 0 8px;
  font-size: 18px;
}

.confirm-dialog-card p {
  margin: 0 0 20px;
  color: var(--muted);
  font-size: 13px;
  line-height: 1.55;
}

.confirm-dialog-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}

.leave-button {
  border: 0;
  border-radius: 14px;
  color: white;
  background: #6854C7;
  font-weight: 850;
}

@media (max-width: 390px) {
  .home-user-card.is-selected {
    align-items: center;
    flex-direction: row;
  }

  .home-user-card.is-selected .home-user-select-wrap {
    width: auto;
  }
}


/* ======================================================
   v2.10 shared ledger management
====================================================== */

.ledger-lock-banner {
  display: flex;
  align-items: center;
  width: 100%;
  gap: 11px;
  margin: 0 0 12px;
  padding: 12px 14px;
  border: 1px solid #DED9F7;
  border-radius: 17px;
  color: #51419E;
  background: #F6F3FF;
  text-align: left;
}

.ledger-lock-banner > span:first-child {
  font-size: 20px;
}

.ledger-lock-banner strong,
.ledger-lock-banner small {
  display: block;
}

.ledger-lock-banner strong {
  margin-bottom: 3px;
  font-size: 13px;
}

.ledger-lock-banner small {
  color: #7A70A8;
  font-size: 10px;
  line-height: 1.4;
}

.nav-add[disabled] {
  cursor: not-allowed;
  color: #A7ADBC;
  background: #E6E8EF;
  box-shadow: none;
  transform: none;
}

.settings-tool-section {
  margin-top: 18px;
  padding: 15px;
  border: 1px solid #E5E8F1;
  border-radius: 18px;
  background: #FAFBFD;
}

.settings-tool-heading,
.settings-tool-title {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
}

.settings-tool-heading > div,
.settings-tool-title > div {
  min-width: 0;
}

.settings-tool-label,
.settings-tool-heading strong,
.settings-tool-heading small,
.settings-tool-title strong {
  display: block;
}

.settings-tool-label {
  margin-bottom: 3px;
  color: var(--primary);
  font-size: 10px;
  font-weight: 850;
}

.settings-tool-heading strong,
.settings-tool-title strong {
  font-size: 14px;
}

.settings-tool-heading small {
  margin-top: 3px;
  color: var(--muted);
  font-size: 10px;
  line-height: 1.4;
}

.ledger-toggle-button {
  flex: 0 0 auto;
  min-height: 42px;
  padding: 0 13px;
  border: 1px solid #D7DCF0;
  border-radius: 13px;
  color: #5F4FB8;
  background: white;
  font-size: 12px;
  font-weight: 850;
}

.ledger-toggle-button.is-locked {
  border-color: #CFC7F2;
  color: white;
  background: #6B59C7;
}

.export-button-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 9px;
  margin-top: 12px;
}

.export-button {
  min-height: 64px;
  padding: 11px 12px;
  border: 1px solid #E0E4EF;
  border-radius: 15px;
  color: var(--text);
  background: white;
  text-align: left;
}

.export-button span,
.export-button small {
  display: block;
}

.export-button span {
  margin-bottom: 4px;
  color: var(--primary);
  font-size: 13px;
  font-weight: 900;
}

.export-button small {
  color: var(--muted);
  font-size: 10px;
}

.transfer-row {
  grid-template-columns: minmax(0, 1fr) auto;
}

.transfer-side {
  display: grid;
  justify-items: end;
  gap: 7px;
}

.transfer-complete-button {
  min-height: 30px;
  padding: 0 10px;
  border: 1px solid #DCE1EC;
  border-radius: 999px;
  color: #798196;
  background: #F7F8FB;
  font-size: 10px;
  font-weight: 850;
}

.transfer-row.is-complete {
  border: 1px solid #D5EADC;
  background: #F3FAF6;
}

.transfer-row.is-complete .transfer-route,
.transfer-row.is-complete > .transfer-side > strong {
  color: #708078;
}

.transfer-row.is-complete .transfer-complete-button {
  border-color: #C5E3D0;
  color: #217B58;
  background: #E8F7EE;
}

.transfer-row.is-complete .transfer-route {
  text-decoration: line-through;
  text-decoration-thickness: 1px;
}

.detail-audit {
  display: grid;
  gap: 5px;
  margin-top: 14px;
  padding: 12px 13px;
  border-radius: 14px;
  color: var(--muted);
  background: #F7F8FB;
  font-size: 10px;
  line-height: 1.45;
}

.detail-audit span {
  display: flex;
  justify-content: space-between;
  gap: 12px;
}

.detail-audit strong {
  color: #606A80;
  font-weight: 800;
  text-align: right;
}

.detail-actions.locked {
  grid-template-columns: 1fr;
}

.detail-locked-message {
  padding: 12px 13px;
  border: 1px solid #DFDAF7;
  border-radius: 14px;
  color: #5E4EB1;
  background: #F6F3FF;
  text-align: center;
  font-size: 11px;
  font-weight: 750;
}

.undo-toast {
  position: fixed;
  left: 50%;
  bottom: calc(96px + env(safe-area-inset-bottom));
  z-index: 130;
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: min(420px, calc(100% - 30px));
  gap: 14px;
  padding: 11px 12px 11px 15px;
  transform: translate(-50%, 22px);
  border-radius: 16px;
  color: white;
  background: rgba(28, 34, 48, .96);
  box-shadow: 0 16px 40px rgba(18,25,42,.24);
  opacity: 0;
  pointer-events: none;
  transition: opacity .2s ease, transform .2s ease;
}

.undo-toast.show {
  opacity: 1;
  transform: translate(-50%, 0);
  pointer-events: auto;
}

.undo-toast span {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 12px;
  font-weight: 750;
}

.undo-toast button {
  flex: 0 0 auto;
  min-height: 34px;
  padding: 0 11px;
  border: 0;
  border-radius: 11px;
  color: #30246F;
  background: #E8E3FF;
  font-size: 11px;
  font-weight: 900;
}

@media (max-width: 390px) {
  .settings-tool-heading {
    align-items: stretch;
    flex-direction: column;
  }

  .ledger-toggle-button {
    width: 100%;
  }
}


/* ======================================================
   v2.12 compact settlement + creator easter egg
====================================================== */

.view[data-view="settlement"] .section-heading:first-child {
  margin-bottom: 12px;
}

.view[data-view="settlement"] .section-heading:first-child h2 {
  font-size: 23px;
}

.view[data-view="settlement"] .settlement-total-card {
  padding: 17px 19px;
  border-radius: 21px;
}

.view[data-view="settlement"] .settlement-total-card span {
  font-size: 10px;
}

.view[data-view="settlement"] .settlement-total-card strong {
  margin: 5px 0 6px;
  font-size: 27px;
  line-height: 1.12;
}

.view[data-view="settlement"] .settlement-total-card p {
  font-size: 10px;
}

.view[data-view="settlement"] .section-heading.mini {
  margin-top: 17px;
  margin-bottom: 9px;
}

.view[data-view="settlement"] .section-heading.mini h3 {
  font-size: 15px;
}

.view[data-view="settlement"] .transfer-list {
  gap: 7px;
}

.view[data-view="settlement"] .transfer-row {
  min-height: 58px;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 8px;
  padding: 9px 11px 9px 13px;
  border-radius: 15px;
}

.view[data-view="settlement"] .transfer-route {
  min-width: 0;
  overflow: hidden;
  gap: 6px;
  font-size: 12px;
  line-height: 1.2;
  white-space: nowrap;
}

.view[data-view="settlement"] .transfer-route span:not(.transfer-arrow) {
  overflow: hidden;
  min-width: 0;
  text-overflow: ellipsis;
}

.view[data-view="settlement"] .transfer-arrow {
  flex: 0 0 auto;
  font-size: 11px;
}

.view[data-view="settlement"] .transfer-side {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 7px;
}

.view[data-view="settlement"] .transfer-side > strong {
  font-size: 13px;
  line-height: 1;
  white-space: nowrap;
}

.view[data-view="settlement"] .transfer-complete-button {
  min-width: 50px;
  min-height: 28px;
  padding: 0 8px;
  border-radius: 10px;
  font-size: 9px;
  white-space: nowrap;
}

.view[data-view="settlement"] .person-summary {
  gap: 7px;
}

.view[data-view="settlement"] .person-row {
  min-height: 62px;
  padding: 11px 13px;
  border-radius: 15px;
}

.view[data-view="settlement"] .person-numbers {
  gap: 8px;
  font-size: 10px;
}

#transferProgressBadge {
  min-width: 58px;
  text-align: center;
}

/* clickable creator easter egg */
.creator-easter-button {
  position: relative;
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 68px;
  gap: 12px;
  overflow: hidden;
  margin: 18px 0 4px;
  padding: 11px 13px;
  border: 1px solid #DED6FF;
  border-radius: 19px;
  color: #3F337A;
  background:
    radial-gradient(circle at 88% 18%, rgba(255,255,255,.95), transparent 23%),
    linear-gradient(135deg, #F3EFFF, #FFF2F8);
  box-shadow: 0 11px 24px rgba(103,83,190,.12);
  text-align: left;
  isolation: isolate;
}

.creator-easter-button::before {
  position: absolute;
  top: -120%;
  left: -38%;
  z-index: -1;
  width: 48%;
  height: 320%;
  transform: rotate(22deg);
  background: linear-gradient(90deg, transparent, rgba(255,255,255,.82), transparent);
  content: "";
  animation: creator-shine 3.2s ease-in-out infinite;
}

.creator-easter-button:active {
  transform: scale(.985);
}

.creator-easter-icon {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 43px;
  height: 43px;
  border-radius: 15px;
  background: rgba(255,255,255,.82);
  box-shadow: 0 7px 16px rgba(103,83,190,.12);
  font-size: 23px;
  animation: creator-hop 2.6s ease-in-out infinite;
}

.creator-easter-copy {
  min-width: 0;
  flex: 1;
}

.creator-easter-copy strong,
.creator-easter-copy small {
  display: block;
}

.creator-easter-copy strong {
  margin-bottom: 3px;
  font-size: 14px;
  font-weight: 900;
}

.creator-easter-copy small {
  color: #8A7EAC;
  font-size: 10px;
}

.creator-easter-arrow {
  color: #8D7FCC;
  font-size: 26px;
  line-height: 1;
}

@keyframes creator-shine {
  0%, 60% { left: -48%; opacity: 0; }
  72% { opacity: 1; }
  100% { left: 120%; opacity: 0; }
}

@keyframes creator-hop {
  0%, 82%, 100% { transform: translateY(0) rotate(0); }
  88% { transform: translateY(-4px) rotate(-5deg); }
  94% { transform: translateY(0) rotate(4deg); }
}

/* creator modal */
.creator-dialog {
  width: min(382px, calc(100% - 30px));
  max-width: none;
  padding: 0;
  border: 0;
  border-radius: 28px;
  background: transparent;
}

.creator-dialog[open] {
  animation: creator-dialog-in .28s cubic-bezier(.22, 1, .36, 1) both;
}

.creator-dialog::backdrop {
  background: rgba(23, 27, 42, .52);
  backdrop-filter: blur(7px);
  animation: sheet-backdrop-in .22s ease-out both;
}

.creator-card {
  position: relative;
  overflow: hidden;
  padding: 24px 21px 20px;
  border: 1px solid rgba(255,255,255,.9);
  border-radius: 28px;
  background:
    radial-gradient(circle at 15% 2%, #FFFFFF 0, transparent 31%),
    linear-gradient(160deg, #FFF9FB, #F4F1FF 70%, #EEE9FF);
  box-shadow: 0 28px 80px rgba(27, 31, 53, .25);
  text-align: center;
}

.creator-card::before,
.creator-card::after {
  position: absolute;
  border-radius: 50%;
  content: "";
  pointer-events: none;
}

.creator-card::before {
  top: -44px;
  right: -38px;
  width: 130px;
  height: 130px;
  background: rgba(255, 183, 213, .2);
}

.creator-card::after {
  bottom: 56px;
  left: -42px;
  width: 105px;
  height: 105px;
  background: rgba(124, 108, 235, .12);
}

.creator-close-button {
  position: absolute;
  top: 13px;
  right: 13px;
  z-index: 3;
  width: 39px;
  height: 39px;
  border: 0;
  border-radius: 13px;
  color: #7A718E;
  background: rgba(255,255,255,.8);
  box-shadow: 0 6px 16px rgba(48,39,91,.08);
  font-size: 17px;
}

.creator-sticker {
  position: relative;
  z-index: 1;
  width: min(245px, 74vw);
  margin: -4px auto -6px;
  filter: drop-shadow(0 13px 16px rgba(45,36,83,.12));
}

.creator-sticker svg {
  display: block;
  width: 100%;
  height: auto;
}

.creator-made-badge {
  position: relative;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  min-height: 24px;
  padding: 0 10px;
  border-radius: 999px;
  color: #6C56C6;
  background: rgba(255,255,255,.78);
  box-shadow: 0 5px 15px rgba(72,56,139,.08);
  font-size: 9px;
  font-weight: 900;
  letter-spacing: .1em;
}

.creator-kicker {
  position: relative;
  z-index: 1;
  margin: 12px 0 4px;
  color: #8D82A5;
  font-size: 11px;
  font-weight: 750;
}

.creator-card h2 {
  position: relative;
  z-index: 1;
  margin: 0;
  color: #30265E;
  font-size: 24px;
  letter-spacing: -.03em;
}

.creator-message {
  position: relative;
  z-index: 1;
  margin: 9px auto 17px;
  color: #776E8B;
  font-size: 12px;
  line-height: 1.55;
}

.creator-confirm-button {
  position: relative;
  z-index: 1;
  width: 100%;
  min-height: 49px;
  border: 0;
  border-radius: 15px;
  color: white;
  background: linear-gradient(135deg, #725DE3, #9A63D8);
  box-shadow: 0 11px 22px rgba(113,88,211,.22);
  font-size: 14px;
  font-weight: 900;
}

@keyframes creator-dialog-in {
  from {
    opacity: 0;
    transform: translateY(16px) scale(.94);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@media (max-width: 360px) {
  .view[data-view="settlement"] .transfer-row {
    padding-left: 11px;
  }

  .view[data-view="settlement"] .transfer-side {
    gap: 5px;
  }

  .view[data-view="settlement"] .transfer-side > strong {
    font-size: 12px;
  }

  .view[data-view="settlement"] .transfer-complete-button {
    min-width: 44px;
    padding: 0 6px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .creator-easter-button::before,
  .creator-easter-icon,
  .creator-dialog[open] {
    animation: none;
  }
}


/* ======================================================
   v2.13 quick trip title editing
====================================================== */

.topbar-brand {
  min-width: 0;
  flex: 1;
}

.trip-title-display {
  min-width: 0;
}

.trip-title-button {
  display: inline-flex;
  align-items: center;
  max-width: 100%;
  gap: 7px;
  padding: 1px 0;
  border: 0;
  color: var(--text);
  background: transparent;
  text-align: left;
}

.trip-title-button > span:first-child {
  overflow: hidden;
  min-width: 0;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 22px;
  font-weight: 900;
  letter-spacing: -.035em;
  line-height: 1.25;
}

.trip-title-edit-icon {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 25px;
  height: 25px;
  border-radius: 9px;
  color: var(--primary);
  background: var(--surface-soft);
  font-size: 11px;
  opacity: .82;
  transition: transform .16s ease, background .16s ease;
}

.trip-title-button:active .trip-title-edit-icon {
  transform: scale(.92);
  background: #E7EBFF;
}

.trip-title-button[disabled] {
  cursor: default;
}

.trip-title-button[disabled] .trip-title-edit-icon {
  display: none;
}

.trip-title-editor {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  width: min(100%, 390px);
  gap: 7px;
  margin-top: 3px;
}

.trip-title-editor input {
  min-width: 0;
  min-height: 41px;
  padding: 0 11px;
  border: 1px solid #9EACFF;
  border-radius: 12px;
  background: white;
  box-shadow: 0 0 0 3px rgba(79,107,255,.09);
  font-size: 13px;
  font-weight: 800;
}

.trip-title-editor-actions {
  display: flex;
  gap: 5px;
}

.trip-title-save-button,
.trip-title-cancel-button {
  min-height: 39px;
  padding: 0 9px;
  border-radius: 11px;
  font-size: 10px;
  font-weight: 850;
  white-space: nowrap;
}

.trip-title-save-button {
  border: 0;
  color: white;
  background: var(--primary);
}

.trip-title-cancel-button {
  border: 1px solid #E0E4EE;
  color: #778095;
  background: white;
}

.trip-title-editor.is-saving input,
.trip-title-editor.is-saving button {
  pointer-events: none;
  opacity: .62;
}

.creator-card h2 {
  margin-bottom: 17px;
}

@media (max-width: 390px) {
  .topbar {
    gap: 10px;
  }

  .trip-title-button > span:first-child {
    font-size: 20px;
  }

  .trip-title-editor {
    grid-template-columns: 1fr;
  }

  .trip-title-editor-actions {
    justify-content: flex-start;
  }

  .trip-title-save-button,
  .trip-title-cancel-button {
    min-height: 34px;
  }
}


/* ======================================================
   v2.14 global creator easter egg
====================================================== */

/* The old settings-only trigger is intentionally removed. */
.creator-easter-button {
  display: none !important;
}

.global-easter-button {
  position: fixed;
  right: max(16px, calc((100vw - 520px) / 2 + 16px));
  bottom: calc(89px + env(safe-area-inset-bottom));
  z-index: 42;
  display: grid;
  place-items: center;
  width: 45px;
  height: 45px;
  padding: 0;
  border: 1px solid rgba(255,255,255,.9);
  border-radius: 16px;
  color: #5E4CB3;
  background:
    radial-gradient(circle at 30% 22%, rgba(255,255,255,.98), transparent 31%),
    linear-gradient(145deg, #F4EFFF, #FFDFF0);
  box-shadow:
    0 10px 25px rgba(77,62,153,.22),
    inset 0 0 0 1px rgba(117,91,209,.06);
  isolation: isolate;
  transition: transform .16s ease, box-shadow .16s ease;
}

.global-easter-button::before {
  position: absolute;
  inset: -5px;
  z-index: -1;
  border: 1px solid rgba(132,103,219,.17);
  border-radius: 20px;
  content: "";
  animation: mystery-ring 2.7s ease-out infinite;
}

.global-easter-button:active {
  transform: scale(.9) rotate(-5deg);
  box-shadow: 0 5px 14px rgba(77,62,153,.18);
}

.global-easter-question {
  font-family: ui-rounded, "Arial Rounded MT Bold", system-ui, sans-serif;
  font-size: 22px;
  font-weight: 950;
  line-height: 1;
  text-shadow: 0 2px 0 rgba(255,255,255,.8);
  animation: mystery-bounce 2.8s ease-in-out infinite;
}

.global-easter-spark {
  position: absolute;
  color: #956FD1;
  font-size: 10px;
  line-height: 1;
  pointer-events: none;
}

.global-easter-spark.spark-one {
  top: 5px;
  right: 6px;
  animation: mystery-spark 2s ease-in-out infinite;
}

.global-easter-spark.spark-two {
  bottom: 6px;
  left: 6px;
  animation: mystery-spark 2s .75s ease-in-out infinite;
}

@keyframes mystery-ring {
  0% {
    opacity: .6;
    transform: scale(.86);
  }
  70%, 100% {
    opacity: 0;
    transform: scale(1.25);
  }
}

@keyframes mystery-bounce {
  0%, 80%, 100% {
    transform: translateY(0) rotate(0);
  }
  86% {
    transform: translateY(-4px) rotate(-8deg);
  }
  93% {
    transform: translateY(1px) rotate(6deg);
  }
}

@keyframes mystery-spark {
  0%, 100% {
    opacity: .25;
    transform: scale(.65) rotate(0);
  }
  50% {
    opacity: 1;
    transform: scale(1.15) rotate(25deg);
  }
}

.creator-original-image {
  position: relative;
  z-index: 1;
  overflow: hidden;
  width: min(270px, 76vw);
  margin: 3px auto 2px;
  border-radius: 20px;
  background: white;
  box-shadow: 0 13px 28px rgba(45,36,83,.13);
}

.creator-original-image img {
  display: block;
  width: 100%;
  height: auto;
  object-fit: contain;
  background: white;
}

/* Prevent stale SVG styles from displaying anything. */
.creator-sticker {
  display: none !important;
}

.creator-made-badge {
  margin-top: 10px;
}

.creator-kicker {
  margin-top: 11px;
}

.creator-card h2 {
  margin-bottom: 17px;
}

@media (max-width: 540px) {
  .global-easter-button {
    right: 16px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .global-easter-button::before,
  .global-easter-question,
  .global-easter-spark {
    animation: none;
  }
}


/* ======================================================
   v2.15 home summary polish
====================================================== */

.summary-grid {
  grid-template-columns: minmax(0, 1fr);
  gap: 10px;
}

.personal-summary-card {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 1px minmax(0, 1fr);
  align-items: stretch;
  min-height: 82px;
  padding: 13px 14px;
  border: 1px solid rgba(229, 233, 242, .78);
  border-radius: 19px;
  background: var(--surface);
  box-shadow: 0 8px 26px rgba(36, 49, 92, .055);
}

.personal-summary-item {
  display: flex;
  min-width: 0;
  flex-direction: column;
  justify-content: center;
  gap: 8px;
  padding: 0 2px;
}

.personal-summary-item:first-child {
  padding-right: 13px;
}

.personal-summary-item:last-child {
  padding-left: 13px;
}

.personal-summary-label {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--muted);
  font-size: 11px;
  font-weight: 720;
  line-height: 1.2;
}

.personal-summary-mark {
  display: inline-block;
  flex: 0 0 auto;
  width: 7px;
  height: 7px;
  border-radius: 50%;
}

.personal-summary-mark.paid {
  background: #7183F5;
  box-shadow: 0 0 0 4px rgba(113,131,245,.1);
}

.personal-summary-mark.owed {
  background: #A49ACB;
  box-shadow: 0 0 0 4px rgba(164,154,203,.11);
}

.personal-summary-item strong {
  overflow: hidden;
  color: var(--text);
  font-size: clamp(16px, 4.7vw, 20px);
  font-weight: 900;
  letter-spacing: -.035em;
  line-height: 1.1;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-variant-numeric: tabular-nums;
}

.personal-summary-divider {
  width: 1px;
  min-height: 52px;
  align-self: center;
  background: linear-gradient(
    to bottom,
    transparent,
    #E5E8F0 18%,
    #E5E8F0 82%,
    transparent
  );
}

.balance-summary-card {
  min-height: 78px;
  padding: 14px 15px;
}

.balance-summary-card > strong {
  margin-top: 3px;
  font-size: clamp(18px, 5vw, 21px);
  font-variant-numeric: tabular-nums;
}

#homeTotal,
#myPaid,
#myOwed,
#myBalance,
.expense-amount strong,
.detail-amount {
  font-variant-numeric: tabular-nums;
}

.expense-amount strong {
  max-width: 112px;
  overflow: hidden;
  font-size: clamp(12px, 3.6vw, 14px);
  letter-spacing: -.025em;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Avoid showing the selected name twice in the compact user card. */
.home-user-card.is-selected .home-user-copy > strong {
  color: #515B72;
  font-size: 12px;
}

/* Keep the mystery trigger available without covering expense amounts. */
.global-easter-button {
  top: 53%;
  right: max(-5px, calc((100vw - 520px) / 2 - 5px));
  bottom: auto;
  width: 39px;
  height: 50px;
  transform: translateY(-50%);
  border-radius: 16px 0 0 16px;
  opacity: .9;
}

.global-easter-button:hover,
.global-easter-button:focus-visible {
  opacity: 1;
}

.global-easter-button:active {
  transform: translateY(-50%) scale(.91) rotate(-4deg);
}

.global-easter-button::before {
  inset: -4px -2px -4px -5px;
  border-radius: 19px 0 0 19px;
}

@media (max-width: 370px) {
  .personal-summary-card {
    padding-right: 11px;
    padding-left: 11px;
  }

  .personal-summary-item:first-child {
    padding-right: 9px;
  }

  .personal-summary-item:last-child {
    padding-left: 9px;
  }

  .personal-summary-item strong {
    font-size: 16px;
  }

  .global-easter-button {
    right: -5px;
  }
}


/* ======================================================
   v2.16 header mystery trigger
====================================================== */

/* Disable the former edge-floating placement. */
.global-easter-button {
  display: none !important;
}

.topbar-actions {
  display: flex;
  align-items: center;
  gap: 7px;
  flex: 0 0 auto;
}

.header-easter-button {
  position: relative;
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 34px;
  height: 34px;
  padding: 0;
  border: 1px solid rgba(255,255,255,.95);
  border-radius: 12px;
  color: #6651BE;
  background:
    radial-gradient(circle at 30% 20%, #FFFFFF 0, transparent 34%),
    linear-gradient(145deg, #F1EDFF, #FFE4F1);
  box-shadow:
    0 7px 18px rgba(80,62,157,.15),
    inset 0 0 0 1px rgba(103,79,193,.05);
  font-family: ui-rounded, "Arial Rounded MT Bold", system-ui, sans-serif;
  font-size: 17px;
  font-weight: 950;
  line-height: 1;
  transition: transform .16s ease, box-shadow .16s ease;
}

.header-easter-button::before {
  position: absolute;
  inset: -3px;
  z-index: -1;
  border: 1px solid rgba(137,105,221,.2);
  border-radius: 15px;
  content: "";
  animation: header-mystery-ring 2.8s ease-out infinite;
}

.header-easter-button:active {
  transform: scale(.9) rotate(-6deg);
  box-shadow: 0 4px 10px rgba(80,62,157,.13);
}

.header-easter-spark {
  position: absolute;
  top: 2px;
  right: 3px;
  color: #A36FC9;
  font-size: 8px;
  animation: header-mystery-spark 1.9s ease-in-out infinite;
}

@keyframes header-mystery-ring {
  0% {
    opacity: .55;
    transform: scale(.85);
  }
  72%, 100% {
    opacity: 0;
    transform: scale(1.24);
  }
}

@keyframes header-mystery-spark {
  0%, 100% {
    opacity: .25;
    transform: scale(.7) rotate(0);
  }
  50% {
    opacity: 1;
    transform: scale(1.15) rotate(24deg);
  }
}

.creator-made-badge {
  letter-spacing: .08em;
}

.creator-confirm-button {
  font-size: 15px;
}

@media (max-width: 370px) {
  .topbar-actions {
    gap: 5px;
  }

  .header-easter-button {
    width: 32px;
    height: 32px;
    border-radius: 11px;
  }

  .sync-status {
    padding-right: 8px;
    padding-left: 8px;
    font-size: 9px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .header-easter-button::before,
  .header-easter-spark {
    animation: none;
  }
}


/* ======================================================
   v2.17 bottom-right mystery trigger
====================================================== */

.header-easter-button {
  display: none !important;
}

/* Leave enough room for the floating trigger near the end of each page. */
.app-main,
main {
  padding-bottom: calc(132px + env(safe-area-inset-bottom));
}

.bottom-easter-button {
  position: fixed;
  right: max(10px, calc((100vw - 520px) / 2 + 10px));
  bottom: calc(88px + env(safe-area-inset-bottom));
  z-index: 43;
  display: grid;
  place-items: center;
  width: 38px;
  height: 38px;
  padding: 0;
  border: 1px solid rgba(255,255,255,.9);
  border-radius: 14px;
  color: #6752BE;
  background:
    radial-gradient(circle at 30% 20%, rgba(255,255,255,.98), transparent 34%),
    linear-gradient(145deg, rgba(241,237,255,.96), rgba(255,228,241,.96));
  box-shadow:
    0 8px 20px rgba(80,62,157,.18),
    inset 0 0 0 1px rgba(103,79,193,.05);
  opacity: .9;
  backdrop-filter: blur(8px);
  transition: opacity .16s ease, transform .16s ease, box-shadow .16s ease;
}

.bottom-easter-button::before {
  position: absolute;
  inset: -3px;
  z-index: -1;
  border: 1px solid rgba(137,105,221,.18);
  border-radius: 17px;
  content: "";
  animation: bottom-mystery-ring 2.8s ease-out infinite;
}

.bottom-easter-button:hover,
.bottom-easter-button:focus-visible {
  opacity: 1;
}

.bottom-easter-button:active {
  transform: scale(.9) rotate(-6deg);
  box-shadow: 0 4px 11px rgba(80,62,157,.14);
}

.bottom-easter-question {
  font-family: ui-rounded, "Arial Rounded MT Bold", system-ui, sans-serif;
  font-size: 18px;
  font-weight: 950;
  line-height: 1;
  animation: bottom-mystery-bounce 2.9s ease-in-out infinite;
}

.bottom-easter-spark {
  position: absolute;
  top: 2px;
  right: 3px;
  color: #A36FC9;
  font-size: 8px;
  animation: bottom-mystery-spark 1.9s ease-in-out infinite;
}

@keyframes bottom-mystery-ring {
  0% {
    opacity: .52;
    transform: scale(.86);
  }
  72%, 100% {
    opacity: 0;
    transform: scale(1.22);
  }
}

@keyframes bottom-mystery-bounce {
  0%, 82%, 100% {
    transform: translateY(0) rotate(0);
  }
  88% {
    transform: translateY(-3px) rotate(-7deg);
  }
  94% {
    transform: translateY(1px) rotate(5deg);
  }
}

@keyframes bottom-mystery-spark {
  0%, 100% {
    opacity: .25;
    transform: scale(.7) rotate(0);
  }
  50% {
    opacity: 1;
    transform: scale(1.12) rotate(24deg);
  }
}

@media (max-width: 540px) {
  .bottom-easter-button {
    right: 9px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .bottom-easter-button::before,
  .bottom-easter-question,
  .bottom-easter-spark {
    animation: none;
  }
}


/* ======================================================
   v2.18 mystery button visibility adjustment
====================================================== */

.bottom-easter-button {
  right: max(12px, calc((100vw - 520px) / 2 + 12px));
  bottom: calc(90px + env(safe-area-inset-bottom));
  width: 45px;
  height: 45px;
  border: 1.5px solid #CFC3F6;
  border-radius: 16px;
  color: #5F49B7;
  background:
    radial-gradient(circle at 29% 19%, #FFFFFF 0, transparent 35%),
    linear-gradient(145deg, rgba(240,235,255,.98), rgba(255,222,239,.98));
  box-shadow:
    0 10px 24px rgba(76,58,151,.23),
    0 0 0 3px rgba(126,98,211,.055),
    inset 0 0 0 1px rgba(255,255,255,.76);
  opacity: .98;
}

.bottom-easter-button::before {
  inset: -4px;
  border-color: rgba(118,88,207,.22);
  border-radius: 20px;
}

.bottom-easter-question {
  font-size: 21px;
}

.bottom-easter-spark {
  top: 3px;
  right: 4px;
  font-size: 9px;
}

.bottom-easter-button:hover,
.bottom-easter-button:focus-visible {
  opacity: 1;
  border-color: #B8A7EE;
  box-shadow:
    0 12px 28px rgba(76,58,151,.27),
    0 0 0 4px rgba(126,98,211,.07),
    inset 0 0 0 1px rgba(255,255,255,.8);
}

@media (max-width: 540px) {
  .bottom-easter-button {
    right: 11px;
  }
}


/* ======================================================
   v2.19 multiple trips
====================================================== */

.trip-title-actions {
  display: flex;
  align-items: center;
  max-width: 100%;
  gap: 5px;
}

.trip-switcher-button {
  min-width: 0;
}

.trip-title-chevron {
  display: inline-grid;
  flex: 0 0 auto;
  place-items: center;
  width: 22px;
  height: 22px;
  color: #6D79A0;
  font-size: 15px;
  line-height: 1;
  transition: transform .16s ease;
}

.trip-switcher-button:active .trip-title-chevron {
  transform: translateY(2px);
}

.trip-quick-edit-button {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 27px;
  height: 27px;
  padding: 0;
  border: 0;
  border-radius: 9px;
  color: var(--primary);
  background: var(--surface-soft);
  font-size: 11px;
}

.trip-quick-edit-button:active {
  transform: scale(.92);
  background: #E7EBFF;
}

.trip-quick-edit-button[disabled] {
  display: none;
}

.trip-switcher-shell {
  padding-bottom: calc(20px + env(safe-area-inset-bottom));
}

.trip-switcher-list {
  display: grid;
  gap: 9px;
  margin-top: 4px;
}

.trip-switch-card {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  width: 100%;
  min-height: 70px;
  gap: 12px;
  padding: 13px 14px;
  border: 1px solid #E3E7F0;
  border-radius: 17px;
  color: var(--text);
  background: #FAFBFD;
  text-align: left;
  transition: border-color .16s ease, background .16s ease, transform .16s ease;
}

.trip-switch-card:active {
  transform: scale(.985);
}

.trip-switch-card.active {
  border-color: #AAB6FF;
  background: #F1F3FF;
  box-shadow: 0 8px 20px rgba(79,107,255,.09);
}

.trip-switch-copy {
  min-width: 0;
}

.trip-switch-copy strong,
.trip-switch-copy small {
  display: block;
}

.trip-switch-copy strong {
  overflow: hidden;
  margin-bottom: 5px;
  font-size: 14px;
  font-weight: 900;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.trip-switch-copy small {
  color: var(--muted);
  font-size: 10px;
}

.trip-active-badge {
  display: inline-flex;
  align-items: center;
  min-height: 27px;
  padding: 0 9px;
  border-radius: 999px;
  color: var(--primary);
  background: white;
  box-shadow: 0 4px 12px rgba(79,107,255,.1);
  font-size: 9px;
  font-weight: 900;
  white-space: nowrap;
}

.trip-switch-arrow {
  color: #A1A9BA;
  font-size: 24px;
  line-height: 1;
}

.new-trip-button {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 68px;
  gap: 12px;
  margin-top: 13px;
  padding: 11px 14px;
  border: 1px dashed #AAB6FF;
  border-radius: 17px;
  color: var(--primary);
  background: #F7F8FF;
  text-align: left;
}

.new-trip-plus {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 40px;
  height: 40px;
  border-radius: 14px;
  color: white;
  background: var(--primary);
  box-shadow: 0 8px 18px rgba(79,107,255,.22);
  font-size: 24px;
}

.new-trip-button strong,
.new-trip-button small {
  display: block;
}

.new-trip-button strong {
  margin-bottom: 3px;
  font-size: 13px;
}

.new-trip-button small {
  color: #7C87AA;
  font-size: 10px;
}

.copy-participants-option {
  display: flex !important;
  align-items: center;
  gap: 11px;
  margin-top: 4px;
  padding: 13px;
  border: 1px solid #E2E6F0;
  border-radius: 16px;
  background: #FAFBFD;
}

.copy-participants-option input {
  flex: 0 0 auto;
  width: 20px;
  height: 20px;
  accent-color: var(--primary);
}

.copy-participants-option > span {
  min-width: 0;
  margin: 0 !important;
}

.copy-participants-option strong,
.copy-participants-option small {
  display: block;
}

.copy-participants-option strong {
  margin-bottom: 3px;
  color: var(--text);
  font-size: 12px;
}

.copy-participants-option small {
  color: var(--muted);
  font-size: 10px;
  line-height: 1.4;
}

.settings-trip-switch-button {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  gap: 12px;
  margin: 2px 0 18px;
  padding: 12px 13px;
  border: 1px solid #E1E5EF;
  border-radius: 16px;
  color: var(--text);
  background: #F8F9FC;
  text-align: left;
}

.settings-trip-switch-button span {
  min-width: 0;
}

.settings-trip-switch-button small,
.settings-trip-switch-button strong {
  display: block;
}

.settings-trip-switch-button small {
  margin-bottom: 3px;
  color: var(--muted);
  font-size: 9px;
}

.settings-trip-switch-button strong {
  overflow: hidden;
  max-width: 190px;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 12px;
}

.settings-trip-switch-button b {
  flex: 0 0 auto;
  color: var(--primary);
  font-size: 10px;
}

@media (max-width: 370px) {
  .trip-title-actions {
    gap: 3px;
  }

  .trip-quick-edit-button {
    width: 25px;
    height: 25px;
  }
}


/* ======================================================
   v2.20 per-trip invitation links
====================================================== */

.trip-share-button {
  display: inline-flex;
  align-items: center;
  min-height: 32px;
  gap: 4px;
  padding: 0 9px;
  border: 1px solid #DDE2F0;
  border-radius: 11px;
  color: var(--primary);
  background: rgba(255,255,255,.86);
  box-shadow: 0 5px 14px rgba(52,68,126,.06);
  font-size: 11px;
}

.trip-share-button span {
  font-size: 14px;
  line-height: 1;
}

.trip-share-button b {
  font-size: 10px;
  font-weight: 850;
}

.trip-share-button:active {
  transform: scale(.96);
}

.share-trip-preview {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px;
  border: 1px solid #E3E7F1;
  border-radius: 18px;
  background: #F8F9FC;
}

.share-trip-icon {
  display: grid;
  flex: 0 0 auto;
  place-items: center;
  width: 44px;
  height: 44px;
  border-radius: 15px;
  background: #EEF1FF;
  font-size: 22px;
}

.share-trip-preview small,
.share-trip-preview strong {
  display: block;
}

.share-trip-preview small {
  margin-bottom: 4px;
  color: var(--muted);
  font-size: 10px;
}

.share-trip-preview strong {
  font-size: 15px;
}

.share-trip-description {
  margin: 12px 2px 16px;
  color: var(--muted);
  font-size: 11px;
  line-height: 1.55;
}

.share-link-field {
  display: block;
  margin-bottom: 10px;
}

.share-link-field > span {
  display: block;
  margin-bottom: 8px;
  font-size: 12px;
  font-weight: 850;
}

.share-link-field > div {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 7px;
}

.share-link-field input {
  min-width: 0;
  font-size: 11px;
}

.share-link-field button {
  min-width: 58px;
  border: 0;
  border-radius: 12px;
  color: white;
  background: var(--primary);
  font-size: 11px;
  font-weight: 850;
}

.native-share-button {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: 62px;
  gap: 12px;
  padding: 11px 14px;
  border: 0;
  border-radius: 17px;
  color: white;
  background: linear-gradient(135deg, #4F6BFF, #765CF2);
  box-shadow: 0 10px 24px rgba(79,107,255,.22);
  text-align: left;
}

.native-share-button > span:first-child {
  font-size: 24px;
}

.native-share-button strong,
.native-share-button small {
  display: block;
}

.native-share-button strong {
  margin-bottom: 3px;
  font-size: 13px;
}

.native-share-button small {
  color: rgba(255,255,255,.76);
  font-size: 10px;
}

.renew-share-link-button {
  width: 100%;
  min-height: 40px;
  margin-top: 9px;
  border: 1px solid #E0E4EE;
  border-radius: 13px;
  color: #727B90;
  background: white;
  font-size: 10px;
  font-weight: 750;
}

.share-security-note {
  margin: 11px 3px 0;
  color: #8A92A5;
  font-size: 9px;
  line-height: 1.5;
}

.join-trip-dialog {
  width: min(380px, calc(100% - 30px));
  padding: 0;
  border: 0;
  border-radius: 25px;
  background: transparent;
}

.join-trip-dialog::backdrop {
  background: rgba(18,25,42,.5);
  backdrop-filter: blur(6px);
}

.join-trip-card {
  padding: 25px 21px 19px;
  border-radius: 25px;
  background: white;
  box-shadow: 0 25px 70px rgba(18,25,42,.22);
  text-align: center;
}

.join-trip-icon {
  display: grid;
  place-items: center;
  width: 52px;
  height: 52px;
  margin: 0 auto 13px;
  border-radius: 17px;
  background: #EEF1FF;
  font-size: 25px;
}

.join-trip-card h2 {
  margin: 4px 0 7px;
  font-size: 20px;
}

.join-trip-card > p:not(.eyebrow):not(.join-trip-help) {
  margin: 0 0 17px;
  color: var(--muted);
  font-size: 12px;
}

.join-trip-card label {
  display: block;
  margin-bottom: 14px;
  text-align: left;
}

.join-trip-card label > span {
  display: block;
  margin-bottom: 8px;
  font-size: 12px;
  font-weight: 850;
}

.join-trip-help {
  margin: 0 0 14px;
  padding: 10px 11px;
  border-radius: 13px;
  color: #6958B8;
  background: #F4F1FF;
  font-size: 10px;
  line-height: 1.5;
}

.join-trip-later-button {
  width: 100%;
  min-height: 38px;
  margin-top: 7px;
  border: 0;
  color: #8A92A4;
  background: transparent;
  font-size: 10px;
  font-weight: 750;
}

.shared-mode-notice {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 0 0 16px;
  padding: 11px 12px;
  border: 1px solid #DDD8F6;
  border-radius: 15px;
  color: #5C4AA9;
  background: #F6F3FF;
}

.shared-mode-notice > span {
  font-size: 18px;
}

.shared-mode-notice strong,
.shared-mode-notice small {
  display: block;
}

.shared-mode-notice strong {
  margin-bottom: 2px;
  font-size: 11px;
}

.shared-mode-notice small {
  color: #7E74A5;
  font-size: 9px;
}

.shared-trip-mode .owner-only {
  display: none !important;
}

.shared-trip-mode #quickEditTripTitleBtn,
.shared-trip-mode .trip-title-chevron {
  display: none !important;
}

.shared-trip-mode #tripSwitcherButton {
  cursor: default;
  pointer-events: none;
}

@media (max-width: 380px) {
  .trip-share-button {
    width: 32px;
    padding: 0;
    justify-content: center;
  }

  .trip-share-button b {
    display: none;
  }

  .topbar-actions {
    gap: 5px;
  }
}


/* ======================================================
   v2.21 MoaTrip branding + location
====================================================== */

.share-trip-location {
  display: block;
  margin-top: 4px;
  color: #6F7890;
  font-size: 10px;
  font-weight: 720;
}

.trip-switch-meta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 4px 7px;
  color: var(--muted);
  font-size: 10px;
}

.trip-switch-location {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  color: #6C5BB0;
  font-weight: 760;
}

.trip-switch-period {
  color: var(--muted);
}

.hero-sub {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 4px 7px;
}

.hero-location {
  color: rgba(255,255,255,.92);
  font-weight: 780;
}

.hero-period {
  color: rgba(255,255,255,.72);
}

.hero-meta-dot {
  color: rgba(255,255,255,.5);
}

@media (max-width: 370px) {
  .trip-switch-meta {
    gap: 3px 5px;
    font-size: 9px;
  }
}
